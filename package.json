{
  "name": "blastup",
  "version": "1.0.0",
  "description": "Blastup — Open-Source WhatsApp Automation Platform by Kalp Intelligence",
  "author": "Kalp Intelligence (https://kalpintelligence.com)",
  "license": "MIT",
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"green,blue\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"green,blue\" \"npm run start --prefix server\" \"npm run start --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
